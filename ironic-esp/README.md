# Ironic-ESP

A simple script that uses docker to build the esp image that Ironic uses to build
ISO's on demand for virtual media. To be used with the published centos9 DIB files
from openstack.

To build, simply run `docker_build.sh`
