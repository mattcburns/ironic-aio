This directory is for SSL/TLS certificates and keys for nginx.

- For development, you can use self-signed certificates (see project README for instructions).
- For production, use certificates from a trusted Certificate Authority (CA).
