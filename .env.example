# Host-side paths for development overrides
# Copy this file to `.env` and adjust paths as needed.

# Explicit config file mounted to /app/ironic.conf (read-only)
IRONIC_CONF=/opt/ironic/config/ironic.conf
IRONIC_DB_FILE=/opt/ironic/db/ironic.sqlite

# vmedia directory served by nginx at /usr/share/nginx/html
IRONIC_VMEDIA_DIR=/opt/ironic/vmedia

# servers directory for static server configuration
IRONIC_SERVERS_DIR=/opt/ironic/servers

# nginx configuration and security files
NGINX_CONF=./nginx.conf
NGINX_HTPASSWD=./htpasswd
NGINX_SSL=./ssl
